#include "dft/hello.hpp"

#include <catch2/catch_test_macros.hpp>
#include <sstream>
#include <functional>
#include <numeric>
#include <vector>
#include "plot/plot.hpp"

SCENARIO("We want to compute the function rectangle", "[fo][basic_functions]")
{
    ///! []
    //using namespace std::placeholders;
    //auto fun = std::bind(, _1, 10.0F);
    //std::vector<float> x(100);
    //std::vector<float> y(100);
    //std::iota(x.begin(), x.end(), -50.0F);
    //std::transform(x.begin(), x.end(), y.begin(), fun);

    //plt::Plot plot;
    //plot.palette("set2");
    //plot.drawCurve(x, y).label("rect(x)").lineWidth(4);
    //plot.save("rect1d.svg");
    ///! []
}
